# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: postgres:16
    restart: always
    volumes:
      - .:/data
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: example

