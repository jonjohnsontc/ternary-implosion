# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    build: .
    restart: always
    volumes:
      - .:/data
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: example
